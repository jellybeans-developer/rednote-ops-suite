#!/usr/bin/env node

process.stderr.write("ZIP packaging was removed in v0.5.0. Install the repository directly with: grok plugin install jellybeans-developer/rednote-ops-suite --trust\n");
process.exit(1);
